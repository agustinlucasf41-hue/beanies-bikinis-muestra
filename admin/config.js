/* Esta URL es pública y no es una credencial. El activador la completa al
   desplegar el Worker; nunca agregues aquí contraseñas ni tokens de GitHub. */
window.BEANIES_ADMIN_CONFIG = Object.freeze({
  apiBase: '',
});
